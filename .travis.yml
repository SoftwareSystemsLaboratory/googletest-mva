language: c
addons:
    apt:
        packages:
        - build-essential
        - cmake

before_script:
    - pushd $TRAVIS_BUILD_DIR && git clone https://github.com/google/googletest && mkdir -p googletest/build && cd googletest/build && cmake .. && sudo make install && cd - && popd

script:
  - cmake .
  - make
  - ./bin/square_root_tests

